import * as React from "react";
import { LoaderWrapper } from "../LoaderWrapper";
import "./SquareRotate.css";

export const SquareRotate = () => {
    return (
        <LoaderWrapper className="square-rotate">
            <div className="square"/>
        </LoaderWrapper>
    );
};
